# SHD Web Prototype — Task 2
Prototype package containing:
- Profile page (index.html)
- Gateway quick view (aside)
- tokens.css (design tokens)
- components.css (basic components)
- js/app.js (bilingual + stubs)

## Next recommended steps (to be executed on SHD workstation)
1. Review the prototype locally: open `index.html` in browser.
2. If satisfied, create a new GitHub repo `shd-web-prototype` and push this folder.
3. To deploy to GitHub Pages:
   - create `gh-pages` branch or enable Pages from `main` with `/` root.
   - ensure `index.html` is at repo root.
4. Update assets (logo SVGs, real fonts) in `/assets/` before public release.
5. Run visual QA on mobile (<=375px) and desktop (>=1280px).

## Release notes (Task 2)
- Implemented brand tokens, typography scale, dark-mode Gateway card.
- Bilingual toggle (ID/EN).
- Signature: "Pikiran Sadar, Sistem Berkembang."

